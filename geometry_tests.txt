y2 = a.x2 + b -> b = y2 - a.x2

y1 = a.x1 + b
y1 = a.x1 + y2 - a.x2
y1 - y2 = a(x1 - x2)
a = (y2 - y1)/(x2 - x1)
b = TODO

m.(y2 - y1)/(x2 - x1) = -1
m = -(x2 - x1)/(y2 - y1)

ym = -xm(x2 - x1)/(y2 - y1) + b
b = ym + xm(x2 - x1)/(y2 - y1)

-----------------------------------

(1, yv).(xb - xa, yb - ya) = 0
(xb - xa) + yv(yb - ya) = 0
yv = -(xb - xa)/(yb - ya)

norm = sqrt(x² + y²)
norm = sqrt(1² + (-dx/dy)²)
norm = sqrt(1² + dx²/dy²)
norm = sqrt((dy² + dx²)/dy²)
norm = sqrt(dx² + dy²)/dy

v = (1, -dx/dy)/norm
v = (1, -dx/dy).dy / sqrt(dx² + dy²)
v = (dy, -dx) / sqrt(dx² + dy²)



(xv, 1).(xb - xa, yb - ya) = 0
xv(xb - xa) + (yb - ya) = 0
xv = -(yb - ya)/(xb - xa)

norm = sqrt(x² + y²)
norm = sqrt((-dy/dx)² + 1²)
norm = sqrt(dy²/dx² + 1)
norm = sqrt((dy² + dx²)/dx²)
norm = sqrt(dx² + dy²)/dx

v = (-dy/dx, 1)/norm
v = (1, -dy/dx, 1).dx / sqrt(dx² + dy²)
v = (dx, -dy) / sqrt(dx² + dy²)
